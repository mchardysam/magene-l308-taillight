# Local-only working files (kept out of the public repo). Opaque on purpose.
private/

# Local community pattern pack the web app optionally loads (it handles absence gracefully).
web/community_patterns/

# Build keys (also covered by the per-folder .gitignore)
garmin/developer_key
garmin/developer_key.pem
garmin-datafield/developer_key
garmin-datafield/developer_key.pem

# Local working backup of the pre-trim (radar) data field — never published
garmin-datafield-full-backup/

# env / build / local
.venv/
node_modules/
__pycache__/
*.pyc
.claude/
# MCP config can hold API tokens — never commit it to this public repo
.mcp.json
.DS_Store
